package functions;

public class EuclidDistance implements Integrate {

	@Override
	public double integrate(double[] val, double[] weights) {
		double [] tmp = new double[val.length];
		for(int)
		return 0;
	}

}
